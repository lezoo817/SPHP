<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.sphp</groupId>
        <artifactId>sphp-server</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>sphp-bootstrap</artifactId>
    <name>sphp-bootstrap</name>
    <description>智愈先锋统一后端启动模块</description>

    <dependencies>
        <!-- 公共基础能力 -->
        <dependency>
            <groupId>com.sphp</groupId>
            <artifactId>sphp-core</artifactId>
        </dependency>

        <!-- B 端业务模块 -->
        <dependency>
            <groupId>com.sphp</groupId>
            <artifactId>B-backend</artifactId>
        </dependency>

        <!-- C 端业务模块 -->
        <dependency>
            <groupId>com.sphp</groupId>
            <artifactId>C-backend</artifactId>
        </dependency>

        <!-- 统一启动模块注册 STOMP WebSocket 配置 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
