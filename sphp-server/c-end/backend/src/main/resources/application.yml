server:
  port: 8082
  servlet:
    context-path: /api

spring:
  application:
    name: sphp-patient-backend

  # ---- PostgreSQL ----
  datasource:
    url: jdbc:postgresql://localhost:5432/sphp
    username: sphp
    password: sphp123
    driver-class-name: org.postgresql.Driver

  # ---- Redis ----
  data:
    redis:
      host: localhost
      port: 6379

  # ---- RabbitMQ ----
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# ---- MyBatis-Plus ----
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# ---- JWT ----
sphp:
  jwt:
    secret: sphp-patient-jwt-secret-change-in-production
    expiration: 7200  # 单位：秒（2小时）

  # ---- 号源预扣超时 ----
  registration:
    payment-timeout: 300  # 支付超时 5 分钟（秒）
