# SPHP Agent 模块开发计划 V4.1

**制定日期**: 2026-08-02
**重构日期**: 2026-08-05（V3 → V4 融入 M9 全面审查结论；V4 → V4.1 已完结内容收敛为一句话留存）
**开发周期**: 预计 12 个工作日（M1-M7 已完成，M8 必改项 2026-08-04 全部完成）
**当前阶段**: 全面审查完成（2026-08-05）→ 真实联调准备
**文档定位**: 开发执行与验收依据；功能契约以《Agent模块系分.md》为准，本文件只描述"做什么、做到什么程度"；已完成里程碑仅一句话留存，明细见附录 A

---

## 一、项目概述

**智愈先锋（SPHP）Agent 模块** — AI 驱动的全链路医疗健康平台的核心 AI 服务，等待与 Java 后端真实连通。

**技术栈**: FastAPI + LangGraph + MCP（来源：系分 §2）

**四层架构**（来源：系分 §4.2）：

| 层 | 目录 | 职责 |
|----|------|------|
| 接入层 | `app/api/` | 对话端点 + JWT 鉴权 + 限流 + traceId |
| 编排层 | `app/orchestrator/` | LangGraph 主图 + 业务子图 + 节点（只调度不实现） |
| 引擎层 | `app/engine/` + `app/mcp_server/` + `app/mcp_client/` | LLM 工厂 + 工具 Schema + RAG + MCP 工具封装 |
| 基础设施层 | `app/infrastructure/` | Java 客户端 + Redis + 配置 + 审计（零业务依赖） |

**核心机制**（来源：系分 §5）：

- 对话流程：前端 JWT → 鉴权 → 意图识别 → [业务子图/RAG] → 回复生成 → SSE 流式
- L1-L4 安全：L1 直接执行 / L2 confirm_token 确认卡 / L3-L4 不注册（双层代码防线，prompt 禁令仅辅助）
- SSE 事件：`message` / `thought` / `action` / `observation` / `card` / `done` / `error`（契约见系分 §6.2.1）
- Java 统一信封：`{code, message, data, traceId}`，成功码**恒为 `"00000"`**（2026-08-05 审查确认代码已全量统一，无 `200` 兼容分支）

**核心边界**（来源：系分 §3 场景定义）：

| 原则 | 含义 |
|------|------|
| 能查不能断 | 可查报告/药典，不可确诊、不可开方 |
| 能建议不能决定 | 可推荐医生/药品，不可替人挂号、替人下单 |
| 能代办不能代确认 | 可发起流程、创建草稿，但敏感操作必须人工确认 |

---

## 二、里程碑总览

| 里程碑 | 目标 | 测试基线 | 状态 |
|--------|------|----------|------|
| M1 最小对话可用 | 发消息 → 收 LLM 回复（SSE 流式） | — | ✅ 完成 |
| M2 工具调用可用 | 查科室 → 返回结果（L1 查询工具 + ReAct） | 50 | ✅ 完成 |
| M3 L2 确认可用 | 创建挂号 → 确认卡片 → 执行成功 | 70 | ✅ 完成 |
| M4 知识库可用 | 医疗知识检索返回结果 | 85 | ✅ 完成 |
| M5 全功能可用 | C 端 4 场景 + B 端 4 场景跑通 | 150 | ✅ 完成 |
| M6 生产就绪 | 集成测试通过，文档完整 | 197 | ✅ 完成 |
| M7 深度审查修复 | 12 P1 + 16 P2 + 7 P3 全部修复 | 322 | ✅ 完成（2026-08-03） |
| M8 联调前必改项 | 重复确认卡 + 健康档案子图 + B 端意图路由/提示词 + 患者上下文（含医院上下文镜像补全） | 414 + 1 跳过 | ✅ 完成（M8-1~M8-5 2026-08-04 + 历史会话列表） |
| M9 全面审查 | 完成度 / 代码质量 / 潜藏 bug 全量评估 | 464 + 1 跳过 + 1 环境 error | ✅ 完成（2026-08-05，结论见第八章，综合 8.5/10，可进入真实联调） |

---

## 三、里程碑详述

### M1-M6：基础设施与业务主线（✅ 已完成，2026-08-02）

| 里程碑 | 关键交付 | 验收 |
|--------|---------|------|
| M1 最小对话可用 | 四节点（auth/intent/rag/reply）、SSE 流式、MCP Server 骨架、工具注册中心（C 30 + B 9） | curl 发消息 → SSE 流式回复 |
| M2 工具调用可用 | tool_caller 双层防护（只绑 L1）、子图循环（tool_caller→safety→executor，≤5 轮）、tool_results 跨轮不覆盖、主图接入 4 子图、SSE action/observation 从 updates 重建 | 查科室 → action → observation → 回复（mock E2E） |
| M3 L2 确认可用 | Redis confirm_token 原子消费（5min TTL）、safety_check 生成卡、card 事件、/chat/confirm 端点、Redis 不可用拒绝 L2（不降级） | 创建挂号 → card → confirm → 执行成功 → 结果返回 |
| M4 知识库可用 | pgvector 检索（score 归一化 + 阈值 0.3）、确定性 ID 去重入库、embedding 选定阿里云百炼 text-embedding-v4 | 真实入库 + 检索 + 去重跑通 |
| M5 全功能可用 | 4 子图白名单分化（5/8/5/5，系分 §5.2.1）、risk_flags 注入回复、B 端 roles 注入 + ADMIN 校验、confirm_done 回执、B 端 9 工具 + 契约表 48 条双校验 | C 端 4 场景 + B 端 4 场景代码完成 |
| M6 生产就绪 | SSE 收敛为 messages+updates、chitchat 独立节点、鉴权单次注入、LLM 供应商链式降级、PostgresSaver 可选、done.usage + thought 事件、MCP Client 内存传输真实协议链路 | 8 任务完成，测试 197 全绿 |

### M7：深度审查修复（✅ 2026-08-03 完成）

第六轮全面审查（4 路并行：安全/编排并发/RAG+MCP/质量契约）发现的问题全部修复、无 P0：12 P1（含匿名 X-Scope 越权、DEBUG 绕过知识库鉴权、Java 失败伪装成功、多轮 L2 卡片覆盖、SSE GeneratorExit、限流突发失效、重复入库去重失效等）+ 16 P2 + 6 P3/规范，测试 218 → 322 全绿，mypy strict 清零；另有验证防线（不修）：Lua 原子消费、glob 转义、Redis fail-closed、confirm 防伪/防重放、L3/L4 双层防护。明细见附录 A。

### M8：联调前必改项（✅ M8-1~M8-5 2026-08-04 全部完成）

第七轮审查发现 2 项必改项 + B 端编排层盘点 3 项缺口，全部完成：**M8-1** 重复确认卡（`_dedupe_tool_calls` 对比 pending 源头拦截 + `safety_check` 复用 token 确定性兜底，并行 L1+L2 确认卡恒为 1 张）；**M8-2** 健康档案子图（`health_graph.py` 10 工具白名单 L1 5 + L2 5，intent 增 health 意图，场景五工具可达）；**M8-3** B 端意图路由（scope=b_end 跳过 C 端意图分类直达工具子图，绑定 B 端全量 9 工具）；**M8-4** B 端系统提示词（`B_TOOL_CALLER_SYSTEM_PROMPT` 医生身份/患者隐私/草稿边界）；**M8-5** 当前患者上下文（A7 确认 `context.patient_id`，AgentState 增 patient_id + `_fill_missing_patient_id` 必填补全，同构补医院上下文注入 + hospital_id 按 scope 解析优先级修复）。M8 收官后另新增**历史会话列表**（`session_store.py` memory/postgres 双实现 + `GET /api/chat/sessions` + `_record_session` 落库钩子，前端 UI 不在本期，接口契约先行）。测试基线 331 → 414 全绿 + 1 跳过。提交记录与验收明细见附录 A。

---

## 四、当前状态与待办

### 4.1 当前基线

| 维度 | 状态 |
|------|------|
| 致命/严重 bug | M7 全部修复（收官）；M8 必改项 5 项全部完成（2026-08-04） |
| 全面审查 | M9 ✅（2026-08-05）：完成度达标、综合评分 8.5/10、可进入真实联调；无未决 P1（详见第八章） |
| 安全 | JWT 校验、限流隔离、PHI 脱敏、confirm 归属 + 时序 + 幂等键、L3/L4 双层防护 |
| 异步模型 | RAG/搜索异步化、Java 瞬时故障重试 |
| ruff / mypy | 全项目 0 errors / strict 清零（app 源码；tests 仅 6 处格式项，见第八章 P3） |
| 测试 | 本地实测 **464 收集，464 全绿 + 1 跳过 + 1 环境 error**（`test_rag_e2e` 缺本地 PG `langchain_pg_embedding` 表，环境前置非代码问题；需真实 PG/Redis 的测试按跳过计） |

> 说明：`tests/` 按 `.gitignore` 不入库，单测为本地验证资产；`docs/Agent/` 入库。

### 4.2 待联调验证（真实连通 checklist，L1-L8）

| # | 项 | 说明 | 前置 |
|---|----|------|------|
| L1 | Java 48 条契约全量联调 | 契约表逐条核对（路径/出入参/信封）；成功码已统一 `00000`（`jwt_auth.py` parse 与 `tool_executor.py` 判定均已收紧，无 200 兼容）；mock 仅覆盖挂号子集，B 端 9 工具与健康档案接口需扩展 mock 或直连 | Java C/B 业务 API |
| L2 | B 端编排联调 | 前提：M8-3/4/5 已完成；实测 9 工具可达性、医生上下文注入、患者切换会话隔离、L2 确认全链路（含 confirm_done 回执） | L1 |
| L3 | JWT 真实校验 | 对接 Java token/parse（成功码 00000 已对齐）；`AUTH_EXPIRED` 需 Java 补过期信号后拆分；无效 token 防刷限流实测 | Java token/parse |
| L4 | 真实 LLM 决策质量 | 意图分类准确率 / 工具选择 / 多轮 L1 自累积 / 防循环（`_dedupe_tool_calls`）实测；探针为 FakeLLM，真实 glm-4 偏保守需实测；多供应商链式降级验证 | LLM key |
| L5 | PostgresSaver 生产验证 | `CHECKPOINTER_BACKEND=postgres`：多 worker 会话共享、重启恢复、session 复用、跨轮状态清场 | PG + pgvector |
| L6 | 知识库 RAG 验证 | pgvector 扩展 + 科普内容填充 + 检索质量调参（top_k/阈值）。⚠️ Windows 开发环境 ProactorEventLoop 与 psycopg 不兼容致 RAG 检索静默降级为空（M9 探针实证），**RAG 验证须在 Linux/容器环境进行** | PG + embedding key |
| L7 | MCP Client 跨进程 stdio | 外部 MCP Server 消费工具的真实形态验证 | — |
| L8 | 并发与故障演练 | 会话锁（引用计数）、滑动窗口限流、confirm 幂等键、Redis 故障 fail-closed 实测 | 压测环境 |

### 4.3 待对齐项

| # | 项 | 状态 |
|---|----|------|
| A1 | X-Scope 对已认证用户全信任：C 端 token 可自报 b_end 触达 B 端工具 → 需 Java B 端 token/parse 返回 roles 后按 roles 绑 scope（header 仅提示） | ⏸ **暂缓**（2026-08-05 决策：先不管；待 B 端 token/parse roles 就绪后按需处理，系分 §11 联动） |
| A2 | B 端无工具白名单（M5 有意设计，全量绑定 9 工具）→ 无工具级防线，仅 L2 + Java 鉴权兜底 | 联调时逐工具确认 Java 侧鉴权 |
| A4 | settings.py 默认 `zhipu_model=glm-4` 与 .env/.env.example 的 glm-4.6v 不一致，删 env 变量会静默换模型 | 改默认值一行，联调前顺手处理 |
| A5 | auth_fail IP 限流副作用：Java 故障时排查请求也被挡（失败关闭）+ NAT 误伤同网段 | 生产加监控告警 |
| A6 | B 端鉴权头辅助 / generate_draft_note 双入口；hospital_id 前端联动 | 系分 §11 已列；context.hospital_id 已确认（前端系分 §9.5，M8-5 同步修复 C/B 端优先级解析） |

已关闭：A3 成功码统一 `00000` ✅（2026-08-05，代码实证收紧，仅需 Java 侧 `Result.success()` 对齐）；A7 context.patient_id 契约 ✅（2026-08-04，前端系分 §9.5，M8-5 已实施）；A8 B 端意图定义 ✅（2026-08-04，随 M8-3 补系分 §5.2）。

### 4.4 已关闭事项（一句话留存）

健康功能（场景五）本期上线 ✅（2026-08-03）；会话持久化介质定案 **PG checkpointer** 不迁 Redis ✅（2026-08-04）；场景六类意图 + health = 7 类 ✅；B 端工具层齐全性核查通过（9 工具与系分 §3.2 完全一致，缺口在编排层已升 M8-3/4/5）✅（2026-08-03）；A3 成功码统一、A1 X-Scope 暂缓 ✅（2026-08-05）。

---

## 五、风险与依赖

### 5.1 外部依赖（来源：系分 §4.1）

| 依赖项 | 提供方 | 状态 | 风险 |
|--------|--------|------|------|
| Java C 端 token/parse | Java 团队 | ✅ 已完成 | 低 |
| Java B 端 token/parse | Java 团队 | ✅ 已完成 | 低 |
| Java C 端业务 API（含健康档案接口） | Java 团队 | ❌ 待开发 | **高** |
| Java B 端业务 API | Java 团队 | ❌ 待开发 | **高** |

### 5.2 技术风险

- MCP Client 内存传输已通过测试；跨进程 stdio 形态待联调验证
- LLM 真实调用延迟 / 意图分类质量需实测
- 并发双击 confirm 依赖 Java 幂等去重兜底（Agent 侧幂等键已贯穿）
- ⚠️ Windows 开发环境：ProactorEventLoop 与 psycopg 不兼容，RAG 检索本地不可用（rag_node 捕获异常后静默降级为空检索，M9 探针实证）；仅影响本地开发调试，Linux 生产/联调环境无此问题，RAG 验证按 4.2-L6 在 Linux 环境进行
- 会话模型：`thread_key = f"{user_id}:{session_id}"` 不含 scope，C/B 端同用户同 session_id 时会话锁与 checkpointer 状态存在串扰面（M9 P2-1），联调期间确认前端 B 端 session_id 生成策略后修复

### 5.3 缓解措施

**Mock 开发模式**: 先对齐 API 契约，Agent 基于契约 Mock 开发，Java API 就位后替换为真实调用。

---

## 六、团队协作

### 与 Java 团队对齐

- 每日同步 Java API 开发进度与接口契约变更
- 每周对齐系分文档更新与联调时间
- 联调前：成功码确认统一 `00000`（A3 已关闭，仅需 Java 侧确认 `Result.success()` 对齐）、B 端 token/parse roles 字段（A1，暂缓）、健康档案 10 工具接口就绪确认

### 与前端团队对齐

- M1-M6 已完成：SSE 流式 / L2 确认卡片 / 全流程测试
- M8 完成后：健康档案场景（场景五）入口联调，SSE 事件字段按 §6.2.1 契约渲染
- 待确认：hospital_id 前端联动（A6）；B 端 session_id 生成策略（关系 thread_key 串扰面，5.2）

---

## 七、配置与部署

### 7.1 关键环境变量（完整键以 `.env.example` 为准）

```bash
JAVA_BASE_URL=http://localhost:8080
LLM_PROVIDER=zhipu                     # zhipu / deepseek / qwen，链式降级
ZHIPU_MODEL=glm-4.6v
DASHSCOPE_EMBEDDING_API_KEY=your_key   # 阿里云百炼 embedding（text-embedding-v4）
PG_HOST=localhost
REDIS_HOST=localhost
CHECKPOINTER_BACKEND=memory            # memory / postgres（生产）
KB_MIN_SCORE=0.3
DEBUG=False                            # 与 ALLOW_ANONYMOUS 互斥（启动校验）
ALLOW_ANONYMOUS=false                  # 生产必须 false
```

### 7.2 启动与部署要点

- 启动依赖链：PostgreSQL(5432) → Java(8080) → Agent(8081) → 前端(8000/8001)
- 目标 PG 必须执行 `CREATE EXTENSION vector`
- 生产：`DEBUG=False` + 关闭 reload，systemd 托管（`Restart=always`）
- 启动双校验：契约表（48 条）+ 工具引用完整性

---

## 八、全面审查结论（M9，2026-08-05）

**审查方式**：全量源码精读（约 8700 行）+ 测试阅读（约 10000 行）+ 门禁验证（pytest / mypy strict / ruff）+ 契约四方交叉核对（工具注册表 ↔ Schema ↔ MCP 映射 ↔ JAVA_API_MAP）+ Schema-函数签名逐一比对 + 运行时探针（FakeLLM + mock dispatch 驱动真实主图验证 SSE 事件序列；会话锁并发引用计数探针）。只审查未改动。

**总评**：完成度高、代码质量过硬，**可进入真实联调**；综合评分 **8.5/10**。安全设计（L1-L4 三层防线）与工程化（契约交叉校验、双后端、fail-closed）是最大亮点。

### 8.1 质量评分

| 维度 | 评分 | 依据 |
|------|------|------|
| 架构分层 | 9.0 | 四层职责清晰，infrastructure 零业务依赖，依赖方向单向 |
| 安全设计 | 8.5 | 三层防线完整、fail-closed 思维到位；扣分：A1 X-Scope 契约点（已暂缓） |
| 健壮性 | 8.0 | 降级策略（RAG 返空、Redis 挂剔除 L2）、指数退避重试、并发锁齐全；扣分：Windows 本地 RAG 不可用 |
| 测试覆盖 | 8.5 | 464 测试覆盖并发/降级/回归/幂等，探针实证 SSE 序列；扣分：集成测试依赖本地 PG |
| 可维护性 | 8.5 | 契约表单一改动点、四层模块 docstring、SSE 契约文档化 |
| 代码质量 | 9.0 | mypy strict 全绿、类型标注完整、错误路径均有日志 |

### 8.2 遗留问题台账（无未决 P1）

**P1 处理结果**：成功码判定不一致 → 已关闭（A3，代码已统一 `00000`）；X-Scope 越权契约点 → 暂缓（A1）；Windows Proactor+psycopg → 降级为技术风险提一嘴（5.2，联调在 Linux 环境验证 RAG）。

**P2（联调期间修复）**：

| # | 问题 | 位置 | 建议 |
|---|------|------|------|
| P2-1 | thread_key 不含 scope，C/B 会话锁与 checkpointer 串扰 | `chat.py` `_get_session_lock`、checkpointer | `thread_key = f"{scope}:{user_id}:{session_id}"`；联调前与前端确认 B 端 session_id 策略（六-前端） |
| P2-2 | memory 后端会话列表不过滤 scope | `session_store.py` | 列表查询按 scope 过滤（与"仅 C 端入列表"规则对齐） |
| P2-3 | 匿名用户与会话表 NOT NULL | PG `agent_sessions.user_id NOT NULL` | 确认 memory 后端无遗漏写入路径（回归测试覆盖） |
| P2-4 | recommend_pharmacies 缺中文标签 | `java_api_map.py` | 补一行标签 |
| P2-5 | 模型默认值不一致（= A4） | `settings.py` | 默认值与 .env.example 对齐 |

**P3（不阻塞）**：ruff tests 目录 6 处格式（I001×2 / E501×4，app 源码 0 错误）；`close_vectorstore` 依赖 PGVector 私有属性 `_async_engine`（已有 try/except 兜底）；mock_java_server 仅覆盖挂号子集（联调需扩展，见 4.2-L1）；test_rag_e2e 依赖本地 PG 建表（CI 文档化环境前置）。

### 8.3 已验证无问题的关键点（探针实证）

- **SSE 事件序列**：probe_e2e 实证 action=2 / observation=1 / card=1 / LLM 调用 4 次；L1→action+observation 配对、L2→action+card 配对、card 恰好一张、seen 签名去重生效
- **会话锁引用计数**：并发探针实证正确释放、无泄漏
- **契约四方一致**：40 注册 = 38 MCP + 2 本地；48 接口零悬空引用；Schema 参数 vs 函数签名零偏差
- **LangGraph 状态管理**：tool_results / pending_confirmations 节点内自累积（不用 operator.add），跨轮 `_build_initial_state` 传 None 清场——历史 bug 已有回归测试
- **安全机制**：L3/L4 双层过滤（schema 不注册 + `_extract_tool_calls` 二次过滤）；confirm_token 幂等键防重复确认；Redis 不可用时 L2 fail-closed 剔除
- **防循环**：`_dedupe_tool_calls` 防 L1 重复调用 + M8-1 防 L2 重复挂起（探针实证 DUP_CARD_CONFIRMED 已修复）

---

## 附录 A：历史明细留存（已完成，2026-08-02 ~ 08-04）

### A.1 M7 修复明细（2026-08-03）

12 P1：Java 失败伪装成功、多轮 L2 卡片覆盖、SSE GeneratorExit、限流突发失效 + 无效 token 洪泛、重复入库去重失效、MCP user_id 注入越权、NaN 相似度污染、同 session 并发竞态、.env 配置风险（DEBUG+ANONYMOUS 互斥）、匿名 X-Scope 越权（NP-1）、DEBUG 绕过知识库鉴权（NP-2）。

16 P2：confirm_done 归属校验、SCAN 冒号前缀越权、jwt userId 缺失判无效、异常脱敏、CORS 收紧、Java 重试 + 幂等键复用、query_reports 并行、向量库生命周期、thread_key 用户命名空间、ingest 大小预检、resolve_api 占位符校验、死参数移除、契约表冗余合并、审计 confirm_method/trigger 真实来源、confirm_done 结果摘要注入、幂等键贯穿 confirm 链路。

6 P3/规范：docstring 补齐、信封 DRY 收敛、_sse_generator 拆分、_execute_mcp 公共化、硬编码入 settings、行内导入提顶。

### A.2 M8 任务明细（2026-08-04，提交记录）

- M8-1（`0429b8d`/`78ea13e`/`c8aa0c7`/`b866970`）：`_dedupe_tool_calls` 增 pending 对比 + pending 摘要注入（`_format_pending`）+ `safety_check` token 复用（`_find_pending` + GET 校验）+ route_continue docstring；回归测试 `test_parallel_l1_l2_only_one_card` 等；331 → 340 全绿
- M8-2（`69a8542`/`3c3d17f`/`876e775`）：`health_graph.py` 10 工具白名单（L1 5 + L2 5）+ intent health 意图与关键词快速通道 + 主图注册/路由/汇聚；`test_health_graph.py` + `test_main_graph.py` 同步；350 → 366 全绿
- M8-3（`b20f0b0`）：主图 auth_node 后按 scope 条件边分叉（`route_by_scope`）→ `b_end_tool_graph` 绑定 B 端全量 9 工具；系分 §5.2 补 B 端意图定义
- M8-4：`B_TOOL_CALLER_SYSTEM_PROMPT`（医生身份 + 隐私脱敏 + 草稿不可签名 + 必带 patient_id），C 端不变；系分 §5.11 同步
- M8-5：A7 确认 `context.patient_id`（前端系分 §9.5）→ AgentState 增 patient_id + `_build_initial_state` 提取 + `_build_patient_context` 注入 + `_fill_missing_patient_id` 必填补全（B 端 5 工具）；顺带 `_resolve_hospital_id` 按 scope 解析优先级 + C 端 5 工具医院上下文注入（`_build_hospital_context`/`_fill_missing_hospital_id`，`_fill_missing_required_param` DRY）；366 → 381 全绿
- 历史会话列表：`session_store.py`（memory/postgres 双实现 + `get_pg_pool()` 池复用）+ `GET /api/chat/sessions`（JWT 隔离、仅 C 端、统一信封）+ `_record_session` 落库钩子（done 前，失败仅 log）+ lifespan 建 `agent_sessions` 表；387 → 414 全绿 + 1 跳过

**M8 验收标准（全部达成）**：health 意图五问跑通（mock E2E）；B 端问诊类提问 9 工具稳定可达不被 qa 吞（M8-3）；B 端医生语义提示词生效（M8-4）；并行 L1+L2 确认卡恒 1 张（M8-1）；全量测试全绿 + mypy/ruff 零错误。

**M8 改动范围**：均为 Agent 模块内部（orchestrator + tests + docs/Agent），不涉及前后端。

---

**制定人**: Claude Code（重构: QoderWork）
**审核人**: 项目负责人
**最后更新**: 2026-08-05（V4.1：已完结内容（M7/M8 详述、4.4 已关闭事项、附录）收敛为一句话留存，明细入附录 A；V4.0：融入 M9 审查结论——A3 成功码统一 00000 关闭、A1 X-Scope 暂缓、Windows Proactor+psycopg 提一嘴，新增第八章审查结论与 4.2 联调 checklist L1-L8，测试基线 464 + 1 跳过 + 1 环境 error）
