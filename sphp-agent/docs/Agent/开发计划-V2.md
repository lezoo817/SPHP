# SPHP Agent 模块开发计划 V2.0

**制定日期**: 2026-08-02
**当前阶段**: M1~M6 全部开发完成；真实环境联调前质量收尾已完成（P1 契约偏差 ①②③ 全部处理 + mypy strict 清零 + 3 项审查缺陷修复），以 mock 为基准的 **218 项测试全绿**
**开发周期**: 预计 12 个工作日

---

## 一、项目概述

**智愈先锋(SPHP) Agent 模块** — AI 驱动的全链路医疗健康平台的核心 AI 服务。

### 技术栈

FastAPI + LangGraph + MCP（Model Context Protocol）

### 四层架构

```
① API 接入层 (app/api/)         - 对话端点 + JWT 鉴权 + 限流
② 编排层 (app/orchestrator/)    - LangGraph 主图 + 子图 + 节点函数
③ 引擎层 (app/engine/)          - LLM 工厂 + 工具 Schema + RAG
④ MCP Server (app/mcp_server/)  - 工具封装（stdio transport）
⑤ 基础设施层 (app/infrastructure/) - Java 客户端 + Redis + 配置
```

### 核心机制

- **对话流程**: 前端 JWT → 鉴权 → 意图识别 → [业务操作/医疗咨询] → [工具调用/RAG] → 回复生成 → SSE 流式
- **L1-L4 安全**: L1 直接执行 / L2 生成 confirm_token 等待确认 / L3-L4 代码硬拦截不注册
- **SSE 事件**: `message`（LLM 输出）/ `action`（工具开始）/ `observation`（工具结果）/ `card`（L2 确认）/ `done`（完成）

### 核心边界

| 原则 | 含义 |
|------|------|
| 能查不能断 | 可查报告/药典，不可确诊、不可开方 |
| 能建议不能决定 | 可推荐医生/药品，不可替人挂号、替人下单 |
| 能代办不能代确认 | 可发起流程、创建草稿，但敏感操作必须人工确认 |

---

## 二、已完成工作（记录）

### M1：最小对话可用 ✅

前端发消息 → Agent 鉴权 → LLM 推理 → SSE 流式返回。核心链路已打通。

- `auth_node` / `intent_node` / `rag_node` / `reply_node` 四节点实现
- SSE 流式（`astream_events` v2，逐 token）
- 健康检查端点（PG / Redis / LLM）
- MCP Server 启动骨架（stdio transport）
- 工具 Schema 注册中心（C 端 30 + B 端 9）

### 代码优化完善 ✅（22 commits）

对全模块做并行代码审查，修复 **6 CRITICAL + 11 HIGH**，并通过 ruff 全项目检查（0 errors）。

**批次1 — 致命 bug（5 commits）**
- `9e71f90` 健康检查对同步函数误用 await 导致 TypeError
- `5676fbd` close_redis 清除 lru_cache 避免返回死连接
- `f4a03e3` java_client 捕获 JSONDecodeError 并修正错误处理契约
- `39269ac` summary 静默吞错加日志 + 删除重复 truncate_messages
- `3eb36ae` 统一 messages 格式访问 + RAG 检索异步化

**批次2 — 安全加固（8 commits）**
- `0000313` JWT 中间件真正校验 token + 限流按用户隔离 + 中间件顺序修正
- `5792c0f` chat scope 优先取中间件注入值
- `b6c8793` 知识库端点路径遍历白名单 + 上传校验 + 鉴权
- `1655765` JWT 始终 401 不降级（D1 严格策略）
- `d794576` MCP 失败终止进程 + CORS 收敛 + 移除硬编码密码（D3 严格策略）
- `d94fb00` 导诊工具补全 hospital_id + 脱敏症状日志

**批次3 — 质量对齐（4 commits）**
- `661ee86` reply 安全声明全意图注入 + 不 mutate state + docstring 修正
- `1a3fb8e` 子图编译 + 提取共享工厂去重 + 补 registration_graph
- `d1374ac` SSE 改 astream_events 实现逐 token 流式 + 拆分生成器 + 错误不泄露
- `795d9d5` 小项（logger import / factory temperature / schemas scope Literal 校验）

**后续清理（5 commits）**
- `cb25abd` 中间件 dispatch 类型注解
- `212dd52` health 提为模块级 + 拆检查辅助 + APP_VERSION 统一
- `3889808` c_schemas / b_schemas 按业务域拆分子函数（39 工具注册验证）
- `9d6918f` 清理未用变量与导入（F841 / F401）
- `bb8242f` SecurityLevel / ToolScope 改用 StrEnum（UP042）
- `369bc6a` ruff import 排序 + mcp tools E501 屏蔽配置

**关键设计决策**

| 决策 | 最终方案 |
|------|---------|
| D1 JWT 校验失败 | 始终返回 401，不降级匿名（严格） |
| D3 MCP 启动失败 | 始终 raise 终止进程（严格） |
| D2 knowledge 鉴权 | 要求登录（B 端 ADMIN 角色待 roles 注入后补） |
| D4 SSE 流式 | 纳入，已用 astream_events 实现 |

---

## 三、当前状态

| 维度 | 状态 |
|------|------|
| 致命/严重 bug | ✅ 6 CRITICAL + 11 HIGH 全部修复 |
| 安全 | ✅ JWT 校验、限流隔离、路径遍历、硬编码密钥、CORS、PHI 脱敏 |
| 异步模型 | ✅ RAG/搜索异步化，同步 I/O 用 to_thread 包装 |
| 函数规范 | ✅ 超长函数拆分，函数 ≤50 行 |
| 类型注解 | ✅ dispatch、端点返回值、StrEnum |
| DRY/不可变 | ✅ 子图去重、reply 不 mutate、限流复用 redis_client |
| ruff 检查 | ✅ 全项目 0 errors |
| mypy | ✅ strict 基线已清零：142 errors → 0（40 文件，泛型补齐 + cast + SecretStr，python_version 对齐 3.13） |

**待联调验证**（依赖 Java token/parse + LLM API）：
- L2 挂号全链路：**代码链路已用 mock 两轮决策单测验证**（子图循环：查排班→注入 slot_id→create_appointment 进 pending），剩余是**真实 LLM 决策**（glm-4 偏保守，原因 3）与跨 session 上下文（测试脚本需复用同一 session_id）——重启 Agent 后待实测
- SSE token 级流式实际输出（单测已用 mock AIMessageChunk 覆盖）
- JWT 真实校验（对接 Java token/parse 接口）

### 本轮核心操作留存（2026-08-03）

**1. Java 接口契约层重构**（为 M5 mock 开发打地基）
- 新建 `app/infrastructure/java_api_map.py`：48 条接口契约（method/path/scope）集中管理，Java 接口变化只改此表
- `call_java_api` 支持 tool_name / api_name+path_params 查表分发，替换原 `NotImplementedError` 死代码
- 8 个工具文件消除 61 处散落路径，改传契约名
- 启动双校验：`validate_contract`（表内合法性）+ `validate_tool_references`（工具引用 vs 契约一致性），接口配置错误 fail-fast

**2. 全面代码审查清理**
- 修复 **CRITICAL**：Redis 故障时 L2 工具被无确认直接执行（安全护栏失效）→ 失败时剔除 L2 调用
- 修复 **HIGH**：token/parse 返回非 dict 时中间件 500
- 修复 MEDIUM：RAG 上下文不入历史（防跨轮残留）、PG 密码 URL 编码、`check_drug_interaction` 参数名、confirm `expires_at`、审计日志 user_id=None
- 清理死代码：8 个空 register 函数、3 个未引用响应模型、未接线配置（llm_api_key/rabbitmq_*）、空 package-lock.json、孤儿 pyc
- 保留：`memory/`（对话记忆）、`prompts/`（角色提示词）为规划功能

**3. 配置修正**
- `.env.example` 补 `DASHSCOPE_EMBEDDING_*`/`KB_MIN_SCORE`/`LOG_LEVEL`
- 修正 `DEFAULT_LLM_PROVIDER`→`LLM_PROVIDER`（原键名与 settings 字段不匹配，配置一直未生效）
- settings 补 `zhipu_embedding_model` 字段（此前缺字段，智谱分支会 AttributeError）

**4. 编排层 10 bug 修复轮**（2026-08-03，`test_live_tools.py` 联调驱动）
- **Bug A（CRITICAL）**：工具真实数据不进 LLM → `_format_tool_results` 解 Java 信封、截断注入，reply 与 tool_caller 共用
- **Bug 1**：`truncate_messages` 兼容 dict/BaseMessage，reply 与 tool_caller 共用 `memory_window_size`
- **Bug 2**：`rag_context` 每轮重置（`_build_initial_state` 补字段 + rag 无命中显式清空）
- **Bug 3/4/6**：`pending_confirmation` dict → `pending_confirmations` 列表（多卡），L1 总保留、SSE 只对 L1 推 action
- **Bug 5**：限流改 Lua 脚本原子「清旧+计数+条件写入」，被拒请求不计数
- **Bug 7**：tool_executor 未注册工具降级失败结果，不抛 KeyError
- **Bug 8**：tool_caller 携带完整对话历史 + 注入上一轮工具结果，子图循环（MAX_TOOL_ITERATIONS=5）支撑多步决策
- **Bug B**：SSE 流式 `in ("ai","AIMessageChunk")`，流式后补推 MEDICAL_DISCLAIMER，`done` 兜底必达
- 配套：SSE action/observation 从 updates 重建（子图 custom 事件不传播），card 事件遍历 pending 列表，确认端点原子消费 token

**5. 遗留低危清理**（2026-08-03）
- auth.py 移除 AgentState 不存在的 `account` 字段（4 处返回 + docstring）
- redis `setex` 弃用 → `set(key, value, ex=ttl)`
- `MEDICAL_DISCLAIMER` 收敛 reply.py 单一来源，chat.py 引用
- ruff format 全量规范化（92 文件 0 error）
- 真实流式路径（AIMessageChunk）单测补覆盖（tests/ 目录本地留存，不入库）

> **说明**：`tests/` 目录按 `.gitignore` 不入库，全部单测为本地验证资产；源码质量以 ruff/mypy 提交前检查把关。

**6. 子图循环验证 + 新 bug 修复**（2026-08-03，复盘驱动）
- **验证**：新增 `test_tool_subgraph_loop.py`（mock LLM 两轮决策）证明子图循环链路可通——第一轮 `query_schedule_slots` 执行拿 slot_id=301 → 注入结果 → 第二轮 `create_appointment` 进 pending。`route_continue` + `MAX_TOOL_ITERATIONS=5` 循环终止正确
- **新 bug（CRITICAL 场景）**：子图循环第 2+ 轮 LLM 未返回新工具调用时，`route_safety` 仍路由到 `tool_executor`，原逻辑返回 `{"tool_results": []}` **覆盖**第一轮查询结果 → reply 拿不到真实数据（Bug A 复现）。修复：无 tool_calls 时返回空 dict 不更新 tool_results（`1a1bca9`）

**7. 核心价值链闭环验证**（2026-08-03，最终联调）
- `tool_results` 自累积（`previous + formatted`，不用 reducer 规避 checkpointer 污染）：多轮 L1 结果不覆盖，SSE 两轮 action/observation 均在（`dcb8370`）
- tool_caller 注入文案改「已执行的工具结果」；prompt 规则 6：拿到参数后直接调 L2 工具，不自然语言反问
- **最终联调全通**（mock Java + 真实 agent）：挂号查询 5 对 action/observation；L2 card 首次触发（`confirm_appointment`）；confirm 回调 Java 返回 LOCKED 订单；confirm_token 一次性消费（重复过期）

---

## 四、下一阶段（M2-M6）

### M2：工具调用可用 ✅

**目标**: LLM 能调 L1 工具查数据，`action`/`observation` 事件正确推送，验收链路 `curl 查科室 → action → observation → 回复` 跑通。

> **M2 范围决策（关键）**：本阶段 **只启用 L1 查询工具**。L2 工具不绑定给 LLM（不进入 prompt），避免 LLM 选中创建型操作后因确认端点未实现而流程挂起。L2 确认链路整体归 M3。`safety_check` 已实现且保留，作为 M3 的现成前置。

**T1. 实现 `tool_caller` 节点**（核心，1 天）— `app/orchestrator/nodes/tool_caller.py` ✅
- 经 `ToolRegistry.get_tools_by_scope(scope)` 取工具，过滤出 `security_level == L1`，`to_openai_schema()` 转 OpenAI 格式
- `llm = build_llm()` 后 `llm.bind_tools(tools)` 绑定
- 用 `get_last_user_content(state)` 取用户消息 + 工具清单描述构造调用 prompt
- `await llm.ainvoke(...)` 解析 `response.tool_calls` → `[{"name", "arguments"}]` 写入 `tool_calls`
- 双层防护：① 只绑定 L1；② 即便 LLM 返回 L2/L3 工具名也过滤掉，只放行 L1
- 兜底：LLM 未选工具 / 解析失败 / 异常 → `tool_calls=[]`，走回复节点（qa 式回答），不阻塞

**T2. 修复 `tool_executor._execute_local` 缺 await**（0.2 天）— `app/orchestrator/nodes/tool_executor.py:94` ✅
- `results = search_knowledge(query=query)` 缺 `await`（`search_knowledge` 为 async 函数，现返回 coroutine 而非结果）
- 改为 `await search_knowledge(query=query)`，补参数缺失时容错
- 添加对应单测（mock `search_knowledge` 验证 await 后拿到真实结果）

**T3. 主图接入工具子图链路**（0.5 天）— `app/orchestrator/graphs/main_graph.py` ✅
- `route_by_intent`：`triage/registration/consultation/pharmacy` → 对应 4 个子图；`qa/chitchat` → `qa_node`
- 注册 4 个子图节点（`triage_graph/registration_graph/consultation_graph/pharmacy_graph`，均为编译后 CompiledGraph）
- 各子图 → `reply_node` 汇聚
- M2 阶段 L2 工具不绑定，故 `pending_confirmation` 不会在流程中出现；子图若异常/空结果由 `reply_node` 兜底

**T4. SSE `action`/`observation` 事件完善**（0.3 天）— `app/api/routes/chat.py` ✅
- ⚠️ **实现调整**：langgraph 1.2.9 **没有 `emit_custom_event`**，且 `tool_executor` 直接调异步函数（非 langchain Tool），`on_tool_start/on_tool_end` 事件**不会触发**（原映射为死代码）
- 改为：`tool_executor` 用 `get_stream_writer()` 推 `tool_action`/`tool_observation` 自定义事件，SSE 层改用 `astream(stream_mode=["custom","messages","updates"])` 混合模式接收
- `custom` 且 `type=tool_action` → `event: action`（含 tool + arguments）；`type=tool_observation` → `event: observation`（含 tool + success）
- `messages` 且 reply_node 的 ai token → `event: message` 逐 token；`updates` 未流式时兜底推完整回复
- 保持 `done` 事件在最后

**T5. 单元测试补齐**（0.5 天）— `tests/unit/orchestrator/` + `tests/unit/api/routes/` ✅
- `test_tool_caller.py`：mock LLM 返回 tool_calls / 无 tool_calls / 返回 L2 工具被过滤 / 异常降级
- `test_main_graph.py`：`build_main_graph()` 可编译、节点齐全（auth/intent/qa/reply + 4 子图）、意图路由参数化断言
- `test_executor.py` 补 `_execute_local` await 用例（mock `get_stream_writer` 绕开 runnable 上下文）
- `test_chat_sse.py`：用假图端到端验证 SSE 生成器 action/observation/兜底 message/done 映射
- 当前 48 个测试全绿，ruff 全项目通过

**T6. 联调验证（mock Java 端到端）**（0.5 天）✅
- ⚠️ **发现并修复断链**：`call_java_api(tool_name=...)` 对工具名分发抛 `NotImplementedError`，MCP 工具执行路径原本是断的
- **T6a 修复 tool_name→封装函数分发**：`_execute_mcp` 加 `_MCP_TOOL_FUNCS` 映射表（37 工具 → mcp_server/tools/*.py 封装函数），按参数名绑定调用，复用已有封装
- **T6b 端到端 SSE 联调**：`tests/integration/test_chat_stream_e2e.py` 用 httpx ASGITransport 直连真实 FastAPI + 真实主图，mock LLM/Java/Redis/RAG，完整走通 `POST /api/chat/stream` 的 `action → observation → message → done` 序列
- ⚠️ **SSE 方案再调整**：langgraph 1.2.9 子图内 `get_stream_writer()` 的自定义事件**不传播**到主图 `custom` stream。SSE 层改为从 `updates` 里的 `tool_calls`/`tool_results` **重建** action/observation 事件（子图最终 state 更新包含完整数据），`custom` 通道保留给未来顶层节点
- 当前 50 个测试全绿（含 E2E），ruff 全项目通过

**M2 阶段暂缓（明确不做）**
- **MCP Client 接入**：`tool_executor` 当前直接调封装函数（过渡实现），已能跑通验收链路；MCP Server 侧工具处理器（`server.py:78`）为空，真正走 MCP `tools/call` 需双向工作，留待 M3/M4 与 MCP SDK 稳定后一并接入
- **L2 确认链路**（confirm_token 生成、`card` 事件推送、`/chat/confirm` 实现）— 归 M3
- **子图业务分化**（4 个子图当前共用同一工具骨架）— 归 M4/M5

**验收**: ✅ `查科室 -> action 事件 -> observation 事件 -> 返回结果`（mock Java E2E 已跑通），`tool_caller`/`route_by_intent`/主图编译/SSE 映射均有测试覆盖，ruff 通过。

### M3：L2 确认可用 ✅

**目标**: 创建型操作走 card 确认 → confirm 回调 → 执行 → 返回结果。全链路（含 E2E）已跑通，64 测试全绿。

**M3-A. Redis confirm_token 按 token 原子消费** — `app/infrastructure/cache/redis_client.py` ✅
- 新增 `get_and_delete_confirm_token_by_token(session_id, token_id, expected_user_id)`
- `/chat/confirm` 只回传 session + token，而 Redis key 是 `confirm:{session}:{tool}:{token}`（tool 未知），用 Lua SCAN 前缀匹配 token_id 后缀 + 校验 user_id + 一次性 GET+DEL
- **key 格式保持不变**（不影响 Java/前端联调对齐）
- 单测（mock eval）+ 真 Redis 集成测试（消费一次 / 非本人拒绝）

**M3-B. safety_check 生成时写 Redis** — `app/orchestrator/nodes/safety.py` ✅
- L2 工具调 `set_confirm_token()` 写 Redis（5min TTL），挂起等待确认
- Redis 不可用时拒绝该 L2 操作（`redis_unavailable_{tool}` 风险标记），不降级为直接执行
- `pending_confirmation` 携带 `session_id`（供 card 事件展示 + confirm 校验）

**M3-C. tool_caller 绑定 L1+L2** — `app/orchestrator/nodes/tool_caller.py` ✅
- 由「只绑定 L1」改为绑定 L1+L2（L3/L4 仍不注册），`_extract_tool_calls` 同步过滤
- 双层防护保持：不注册 + 即便 LLM 返回也过滤
- L2 由 safety_check 拦截生成确认，故绑定后不会在流程中直接执行

**M3-D. SSE 推送 card 事件** — `app/api/routes/chat.py` ✅
- 从 `updates` 的 `pending_confirmation` 重建 `event: card`
- `_build_card()`：card_type / confirm_token / session_id / title / summary / expires_at
- `_TOOL_LABELS` 中文标签（挂号/购药/问诊等 14 个 L2 工具）
- ⚠️ **session_id 一致性修复**：前端不传 session_id 时，入口统一生成并注入 state / generator，保证 `done` 返回的 session_id 与 `card` 事件、Redis confirm key 一致（否则 L2 确认无法配对）

**M3-E. /chat/confirm 端点** — `app/api/routes/chat.py` ✅
- 原子消费 confirm_token（Redis 一次性 GET+DEL + user_id 校验）→ 校验 session → 同步执行对应 L2 工具（`_execute_mcp`）
- 错误码对齐系分 §6.2.2：`CONFIRM_INVALID` / `CONFIRM_EXPIRED` / `SESSION_MISMATCH` / `TOOL_FAILED`
- 成功 `{code:00000, data:{action_result, message}}`，`_success_message()` 逐工具中文提示
- 匿名请求（无 user_id）`getattr` 兜底，不抛 AttributeError

**M3-F. 测试补齐** — `tests/unit/api/routes/test_chat_confirm.py` + `tests/integration/` ✅
- `test_chat_confirm.py` 7 用例：成功 / 参数缺失 / 过期 / 会话不匹配 / Redis 不可用 / 工具失败 / 匿名
- `test_safety.py` 6 用例：L1 放行 / L2 挂起 / card_type 映射（含 join_waitlist、confirm_drug_receipt 回归）/ Redis 故障拒绝 / 空输入
- E2E `test_e2e_l2_confirm_flow`：真 Redis 全链路——SSE 出 `card` → 取 confirm_token → `/chat/confirm` 执行挂号成功 → 重复提交 `CONFIRM_EXPIRED`（token 一次性）
- 当前 70 测试全绿（1 skipped 为 mark 配置），ruff 全项目通过

**验收**: ✅ `创建挂号 → card 事件(confirm_token) → /chat/confirm → 执行成功 → 结果返回`，token 一次性消费，重复提交过期。

### M4：知识库可用 ✅

**目标**: 医疗知识检索返回结果。全链路（真实入库 + 检索 + 去重）已跑通，85 测试全绿。

**M4-A. 修复 vectorstore 异步检索** — `app/engine/rag/vectorstore.py` ✅
- `get_vectorstore` 构造 `PGVector` 缺 `async_mode=True`，只建同步 engine，而 `search_knowledge` 走异步检索 `asimilarity_search`，断言 `_async_engine` 失败
- 改为 `async_mode=True` 走 `create_async_engine`，异步检索链路打通

**M4-B. 检索 score 归一化 + 阈值过滤** — `app/engine/rag/search.py` ✅
- score 截断归一化到 [0,1]（bge-m3 未 unit-norm 时可能越界）
- 新增 `kb_min_score` 阈值（默认 0.3），过滤低相关结果

**M4-C. 检索失败降级 + 空库处理** — `app/engine/rag/search.py` ✅
- `search_knowledge` 捕获内部异常降级返回空列表（embedding / 连接失败不抛异常），`/search` 端点与 `rag_node` 不再 500
- 空库自动建 collection 返回空
- `rag_node` 空结果时不注入误导性上下文（`format_context([])` 返回「未检索到」，原逻辑误当系统知识注入）

**M4-D. 入库确定性 ID 去重 + 异步入库** — `app/engine/rag/ingest.py` ✅
- 每个 chunk 生成确定性 UUID（文件名+内容哈希），PGVector `ON CONFLICT DO UPDATE` upsert，重复入库覆盖不重复；内容变化则 ID 变化
- `get_vectorstore` 改 `async_mode` 后 `_engine` 为 None，`ingest_file`/`ingest_directory` 改 async 用 `aadd_documents`；读盘+切分用 `asyncio.to_thread` 不阻塞事件循环

**M4-E. 阿里云百炼 embedding 接入 + 测试** — `app/engine/rag/embedder.py` ✅
- ⚠️ 硅基流动 key 无效（`30014`）、智谱 embedding 余额不足（`429 1113`），新增 **dashscope**（阿里云百炼 `text-embedding-v4`）为 embedding provider
- 关键：阿里云百炼不接受 langchain 的 tiktoken token ID 输入（报 `input.contents` 缺失），须 `check_embedding_ctx_length=False` 直接传原始文本
- 真实验证：`aembed_query`/`aembed_documents` 返回 1024 维，真实入库+检索 score 0.748
- 新增 `tests/unit/orchestrator/test_rag.py`（8 用例）+ `test_ingest.py`（6 用例）+ `tests/integration/test_rag_e2e.py`（真实 PG + mock embedding 全链路）

**遗留待办**（归 M5）：

- **T-M3-L1. 确认结果回写对话上下文** — `/chat/confirm` 是独立 HTTP 请求，执行成功结果**不进入 graph 状态**。用户确认挂号后，再问「挂号好了吗」时 Agent 不知刚才已成功，对话不连续。
  - 方案：confirm 执行成功后写一条「已确认动作」记录到 Redis（如 `confirm_done:{session}:{tool}`，携带执行结果），下一轮对话初始状态读取该记录注入 `messages` 上下文
  - 验收：确认成功后追问同一会话，Agent 能引用刚才的确认结果

### M5：全功能可用 ✅

**目标**: C 端 4 场景 + B 端 4 场景跑通。5 个任务全部代码完成并提交，150 单测全绿，ruff 0 error。

**M5-T1. 子图业务分化** — `app/orchestrator/graphs/{triage,registration,consult,pharmacy}_graph.py` + `app/orchestrator/nodes/tool_caller.py` + `graphs/_common.py` ✅
- `tool_caller` / `_extract_tool_calls` 新增 `allowed_tools: list[str] | None` 白名单参数，绑定与解析双层过滤
- `build_tool_subgraph(tool_names=None)` 经 `_bind_tool_caller` 闭包注入白名单
- 4 子图定义专属工具集常量：`TRIAGE_TOOLS=5` / `REGISTRATION_TOOLS=8` / `CONSULTATION_TOOLS=5` / `PHARMACY_TOOLS=5`，各场景只绑本场景工具（对齐系分 §5.2.1 工具链）

**M5-T2. risk_flags 注入 reply** — `app/orchestrator/nodes/reply.py` ✅
- `_format_risk_flags()` 将 `redis_unavailable_{tool}` / `blocked_{tool}` 风险标记映射为中文警告，以 system message 注入 LLM 上下文（兑现系分 §7.1）
- Redis 不可用 / L3-L4 拦截场景，用户回复中可见明确风险提示

**M5-T3. B 端权限控制** — `app/api/middleware/jwt_auth.py` + `app/api/routes/knowledge.py` ✅
- 中间件从 token/parse 响应注入 `roles`（默认 `[]`）/ `dept_id` / `doctor_id` / `hospital_id` 到 `request.state`（authed 与 anonymous 分支均注入）
- knowledge 入库端点 `_require_auth(admin_only=True)`：未登录 401，无 ADMIN 角色 403

**M5-T4. 确认结果回写对话上下文**（T-M3-L1，M4 遗留）— `app/api/routes/chat.py` + `app/infrastructure/cache/redis_client.py` ✅
- Redis 新增 `confirm_done:{session}:{tool}:{uuid}`（`confirm_done_ttl=3600`），`set_confirm_done()` / `get_and_delete_confirm_done()`（Lua SCAN+GET+DEL 一次性消费）
- `/chat/confirm` 执行成功后写回执；`/chat/stream` 每轮读取并注入 system message（"本会话此前已确认完成：…"），确认后追问连续
- 注入消息持久在 checkpointer 历史，多轮可引用

**M5-T5. B 端 9 工具可调用 + 契约校正** — `app/infrastructure/java_api_map.py` + `app/engine/tools/b_schemas.py` + `app/orchestrator/nodes/safety.py` ✅
- 契约表 B 端路径校正对齐系分 **V1.1**：patient/drug/department/doctor/schedule 类统一 `/api/b/admin/*`，`generate_draft_note` 保持 `/api/b/doctor/consult/{id}/note`
- `query_patient_history` 安全级别 L1→**L2**（医生授权 + 确认卡片），card_type 映射 `confirm_patient_history`
- 启动双校验 `validate_contract` + `validate_tool_references` 通过（48 条契约无悬空引用）

**MCP 过渡实现澄清**: 工具调用当前直调封装函数（过渡实现），接口契约层已就位（java_api_map），MCP Client 接入归 M6。

**验收**: ✅ C 端 4 场景工具白名单分化 + B 端权限与 9 工具可调用代码完成，150 单测全绿，ruff 0 error。

**M5 遗留（依赖真实环境，统一归联调）**：
- B 端契约路径已对齐系分 V1.1，Java 实际接口如有出入在契约表单一改动点修正
- 子图分化效果、B 端 L2 确认（query_patient_history 卡片）、risk_flags 中文警告需真实 LLM + Java 业务 API 实测
- 联调时间点：**M6 开发完成后统一进行**

### M6：生产就绪 ✅

**目标**: 生产级健壮性。8 个任务全部代码完成并提交，197 测试全绿（单测 192 + E2E 5），ruff 0 error。

**M6-A1. SSE custom 通道定案清理** — `app/api/routes/chat.py` + `app/orchestrator/nodes/tool_executor.py`（`8523f00`）✅
- 确认以 `updates` 重建 action/observation 为既定方案（langgraph 1.2.9 子图 custom 事件不传播到主图）
- stream_mode 由 `["custom", "messages", "updates"]` 收敛为 `["messages", "updates"]`，删除 custom 分支
- tool_executor 移除 `get_stream_writer()` 及其推送，action/observation 统一从子图最终 state 的 `tool_results` 反推

**M6-A2. chitchat_node 独立节点** — `app/orchestrator/nodes/chitchat.py` + `app/orchestrator/graphs/main_graph.py`（`27811bf`）✅
- 闲聊意图走独立节点（仅消费对话历史），与知识问答区分，不触发 RAG 检索
- `route_by_intent` 新增 `"chitchat": "chitchat_node"`，接入主图条件边

**M6-A3. compress_messages 清理** — 删除 `app/engine/memory/summary.py`（`c66a15a`）✅
- 记忆压缩定案：`truncate_messages` 滑动窗口（系分 §5.9）为既定策略；`compress_messages` 零调用零测试，删除避免 O(n²) 全量摘要 LLM 调用与医疗安全幻觉风险

**M6-B1. 鉴权去重** — `app/orchestrator/nodes/auth.py` + `app/api/routes/chat.py`（`3f7db71`）✅
- JWT 中间件 token/parse 单次注入身份（`user_id`/`roles`/`dept_id`/`doctor_id`/`hospital_id`），`auth_node` 消费注入值不再重复调 Java
- 直接图调用（绕过中间件）场景保留 token/parse 兜底

**M6-B2. LLM 降级策略** — `app/engine/llm/factory.py`（`394b339`）✅
- 供应商链式降级：`deepseek → zhipu → qwen`，构建时选首个配置完整 API key 的供应商
- 配置期决议（非运行期包装器），不破坏 LangGraph messages-mode token 流式

**M6-B3. PostgresSaver 生产持久化** — `app/orchestrator/checkpointer.py` + `app/infrastructure/config/settings.py` + `app/main.py`（`b546fc2`）✅
- `checkpointer_backend` 配置项：`memory`（默认）/ `postgres`（生产，多 worker 共享会话）
- postgres 走 `AsyncPostgresSaver` + `AsyncConnectionPool`（连接池全局单例），lifespan 建表 / 关闭释放
- `.env.example` 增补 `CHECKPOINTER_BACKEND` 说明

**M6-B4. done.usage + thought 事件** — `app/api/routes/chat.py`（`ca3c9bd`）✅
- `done` 事件携带本轮 LLM token 用量（`prompt_tokens`/`completion_tokens`/`total_tokens`，从 `usage_metadata` 累积）
- 推理模型思考 token（`additional_kwargs.reasoning_content`）以 `event: thought` 推送（系分 §5.12）

**M6-C1. MCP Client 接入（★）** — `app/mcp_client/` + `app/mcp_server/` + `app/orchestrator/nodes/tool_executor.py`（`32b37d9`）✅
- `tool_executor` 执行链路从直调封装函数改为走真实 MCP 协议：`MCP Client(tools/call) → MCP Server → dispatcher → 封装函数 → Java REST`
- `app/mcp_client/client.py`：`ClientSession` 经同进程内存传输连接自身 `Server`，惰性单例 + lifespan 预连接；连接在独立子任务建立，规避 Starlette 中间件 collapsing task group 退出时的 anyio 取消作用域错乱
- `app/mcp_server/tools/dispatcher.py`：工具分发映射自 tool_executor 迁入，`Server call_tool` 与回退直调共用
- `server.py` 注册真实 `tools/list` + `tools/call` 处理器（`_build_server` 工厂，stdio 线程与内存传输对端共用）
- `_call_mcp_func` 优先走 Client，**连接层失败才回退直调**（工具执行失败不触发，避免同一工具执行两次）
- 传输选型说明：同进程内存传输（非 stdio 子进程）——复用已注册处理器、无子进程生命周期/孤儿进程/启动延迟、E2E 对 `call_java_api` 的 patch 仍在同一进程生效；真正跨进程 stdio 场景留待真实环境联调

**M6 遗留（依赖真实环境，统一归联调）**：
- 真实 LLM 多供应商实测（降级触发、推理 thought token、意图/工具调用质量）
- Java 业务 API 全量联调（C 端 4 场景 + B 端 9 工具、B 端 L2 确认卡片、契约表单一改动点校正）
- PostgresSaver 多 worker 会话共享验证、CHECKPOINTER_BACKEND=postgres 生产验证
- MCP Client 跨进程 stdio 形态验证（外部 MCP Server 消费工具）
- mypy strict 基线：已清零（numpy 存根与 python_version 不匹配经 3.13 对齐解决，142 errors → 0）

**验收**: ✅ M6 全部 8 任务代码完成，197 测试全绿（新增 21 项 M6 测试），ruff 0 error；MCP Client 真实协议链路 E2E 验证通过。

### 真实环境联调前质量收尾（2026-08-03）

**1. 代码审查 3 项缺陷修复**（P1 审查，4 commits）
- `b1be4ba` **B 端工具子图不可达（HIGH）**：业务子图白名单过滤将 B 端 8 个工具全部滤空 → 白名单仅约束 C 端（C 端场景专属），B 端全量绑定
- `cc8b05d` **chat_stream Redis 硬依赖（HIGH）**：Redis 故障导致全部对话请求 500 → confirm_done 回执读取失败降级为空列表（与限流 fail-open、L2 转 risk_flags 降级口径一致）
- `d22e52a` **confirm Lua session_id glob 注入（MEDIUM）**：session_id 未转义可越权 SCAN 读取/删除其他会话确认回执 → `_escape_glob` 转义 glob 元字符（Redis 用反斜杠转义，非 Lua 的 `%`；合法 `-`/`_`/`.` 不受影响）
- `00255ae` checkpointer logger 调用按 ruff format 重排

**2. mypy strict 基线清零**（142 errors → 0，40 文件，6 commits）
- `pyproject.toml` `python_version` 3.11→3.13 对齐运行时，消除 numpy 2.5.1 存根「type statement 需 3.12+」的解析阻断
- 分层清理：orchestrator / api / infrastructure / mcp / engine 全量补齐泛型（`dict`→`dict[str, Any]`、`list`→`list[Any]`、`asyncio.Task`→`Task[Any]`）、`cast()` 收敛 Any 返回（resp.json / json.loads / 动态 loader）、LLM api_key 用 `SecretStr` 包装（对齐 langchain stub）
- 过程中修复 **checkpointer 运行时 NameError**：`_pg_pool` 模块级注解与 `cast(...)` 参数引用 TYPE_CHECKING 下才导入的 `AsyncConnection`，运行期求值崩溃 → `from __future__ import annotations` + `AsyncConnection` 移入 `_build_postgres_saver` 运行时 import
- MCP Server `inputSchema=` → `input_schema=`（对齐已装 mcp SDK 签名，call-arg 修复）

**3. P1 契约偏差处理**（3 commits + knowledge 收敛 3 commits）
- `cdb9c89` 鉴权/限流错误响应对齐系分 §6.1 统一信封 `{code, message, data, traceId}`；错误码 `AUTH_FAILED` → `AUTH_MISSING`（缺 token）/ `AUTH_INVALID`（token 无效）；`AUTH_EXPIRED` 需 Java token/parse 补充过期信号后才能精确拆分（当前归 AUTH_INVALID，同为 401）
- `3f0899b` `/chat/confirm` 错误路径 HTTP 状态码对齐系分 §6.2.2：`CONFIRM_INVALID`/`CONFIRM_EXPIRED`/`SESSION_MISMATCH`=**400**、`TOOL_FAILED`=**500**（原固定 200）；错误路径返回统一信封 JSON
- ✅ **`/api/knowledge/*` 实现收敛到系分 §6.5**（3 commits：`701754e` 引擎 ingest_file + `f560912` 引擎 search + `342be06` API 层）：
  - **合单入库** `POST /api/knowledge/ingest`：file + title（必填）+ category + source，单文件 ≤ **10MB**；成功返回统一信封 `data: {document_id, title, chunk_count, status}`；`document_id` 格式 `doc_日期_随机`（对齐系分示例）
  - **移除对外暴露** `/ingest/file` + `/ingest/directory`；`ingest_directory` 保留为引擎内部批量工具（注释标注不对外）
  - **`/search` 补 category 过滤**（patient_edu / clinical_ref，PGVector JSONB filter）；响应对齐 §6.5.2 字段：results 每项 `rank/score/content/source_doc/source_page/chunk_id` + `total_hits` + `elapsed_ms`
  - **错误统一信封**：`INVALID_REQUEST`(400，格式/超限/title 空/q 空/category 非法) / `AUTH_INVALID`(401) / `FORBIDDEN`(403，非 ADMIN) / `INGEST_FAILED`(500)
  - category 入库为 chunk metadata 标签，为未来前端分类展示与权限控制留基础

**当前验证基线**：mypy 0 errors · ruff check/format 通过 · **218 测试全绿**（新增 P1 修复断言：中间件信封 + confirm 状态码 + knowledge 端点信封）

---

## 五、关键里程碑

| 里程碑 | 目标 | 状态 |
|--------|------|------|
| **M1 最小对话可用** | 发消息 → 收 LLM 回复（SSE 流式） | ✅ 已完成 |
| **M2 工具调用可用** | 查科室 → 返回结果（L1 查询工具 + ReAct） | ✅ 已完成 |
| **M3 L2 确认可用** | 创建挂号 → 确认卡片 → 执行成功 | ✅ 已完成 |
| **M4 知识库可用** | 医疗知识检索返回结果 | ✅ 已完成 |
| **M5 全功能可用** | C 端 4 场景 + B 端 4 场景跑通 | ✅ 已完成 |
| **M6 生产就绪** | 集成测试通过，文档完整 | ✅ 已完成 |

---

## 六、风险与依赖

### 外部依赖

| 依赖项 | 提供方 | 状态 | 风险 |
|--------|--------|------|------|
| Java C 端 token/parse | Java 团队 | ✅ 已完成 | 低 |
| Java B 端 token/parse | Java 团队 | ✅ 已完成 | 低 |
| Java C 端业务 API | Java 团队 | ❌ 待开发 | **高** |
| Java B 端业务 API | Java 团队 | ❌ 待开发 | **高** |

### 技术风险

- MCP Client 已接入（M6-C1），内存传输真实协议链路通过测试；跨进程 stdio 形态待联调验证
- LLM API 响应延迟需监控
- PostgreSQL 需启动 pgvector 扩展

### 缓解措施

**Mock 开发模式**: 先对齐 API 契约，Agent 基于契约 Mock 开发，Java API 就位后替换为真实调用。

---

## 七、团队协作

### 与 Java 团队对齐
- 每日同步 Java API 开发进度与接口契约变更
- 每周对齐系分文档更新与联调时间

### 与前端团队对齐
- M1 完成后：前端集成 SSE 流式输出
- M3 完成后：前端集成 L2 确认卡片
- M5 完成后：端到端全流程测试

---

## 八、配置说明

### 关键环境变量（`.env`）

```bash
JAVA_BASE_URL=http://localhost:8080
ZHIPU_API_KEY=your_key
PG_HOST=localhost
PG_PASSWORD=your_password      # 不再硬编码，必须配置
REDIS_HOST=localhost
CONFIRM_TOKEN_TTL=300
RATE_LIMIT_PER_MINUTE=20
CORS_ORIGINS=["http://localhost:5173"]  # 生产收敛
KB_INGEST_ROOT=/data/kb        # 知识库入库根目录（防路径遍历）
```

### 启动方式

```bash
# 推荐（无热重载）
uvicorn app.main:app --host 0.0.0.0 --port 8081

# 或
python -m app.main
```

### 部署要点

**启动依赖链**: PostgreSQL(5432) → Java(8080) → Agent(8081) → 前端(8000/8001)。Agent 不内嵌 Java，通过 `JAVA_BASE_URL` 调用；与 Java 共用同一套 PG/Redis。

**pgvector 扩展**: 目标 PG 必须执行 `CREATE EXTENSION vector`（向量库首次建表依赖），否则启动报错。

**生产部署**（`DEBUG=False` 必须关闭 reload）:
```bash
nohup .venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8081 > agent.log 2>&1 &
```
推荐 systemd 托管（开机自启 + `Restart=always`），`EnvironmentFile` 指向 `.env`。

**常见问题**: 端口用 `AGENT_HOST/AGENT_PORT`（非旧 HOST/PORT）；MCP 未启用则 `pip install mcp`；连不上 PG 核对 `PG_*`。

---

**制定人**: Claude Code
**审核人**: 项目负责人
**最后更新**: 2026-08-03（真实环境联调前质量收尾：3 项审查缺陷 + mypy strict 清零 + P1 契约偏差 ①②③ 处理；knowledge 接口收敛到系分 §6.5，218 测试全绿）
