# SPHP Agent 模块开发计划 V2.0

**制定日期**: 2026-08-02
**开发周期**: 预计 12 个工作日
**当前阶段**: M1~M6 全部开发完成，质量收尾与遗留清理项已全部处理；2026-08-03 第五轮深度审查（无 P0）发现 10 P1 + 12 P2/P3，同日第六轮全面审查（4 路并行代理）验证 10 P1 + 11 P2 全部真实（契约表冗余 1 项已修复），另发现 **2 项第五轮遗漏的 P1 越权 + 一批新 P2/P3**，进入 M7 深度审查修复，以 mock 为基准 **218 项测试全绿**（ruff/mypy strict 零错误）

---

## 一、项目概述

**智愈先锋(SPHP) Agent 模块** — AI 驱动的全链路医疗健康平台的核心 AI 服务。

### 技术栈

FastAPI + LangGraph + MCP（Model Context Protocol）

### 四层架构

```
① API 接入层 (app/api/)         - 对话端点 + JWT 鉴权 + 限流
② 编排层 (app/orchestrator/)    - LangGraph 主图 + 子图 + 节点函数
③ 引擎层 (app/engine/)          - LLM 工厂 + 工具 Schema + RAG
④ MCP Server (app/mcp_server/)  - 工具封装（stdio transport）
⑤ 基础设施层 (app/infrastructure/) - Java 客户端 + Redis + 配置
```

### 核心机制

- **对话流程**: 前端 JWT → 鉴权 → 意图识别 → [业务操作/医疗咨询] → [工具调用/RAG] → 回复生成 → SSE 流式
- **L1-L4 安全**: L1 直接执行 / L2 生成 confirm_token 等待确认 / L3-L4 代码硬拦截不注册
- **SSE 事件**: `message`（LLM 输出）/ `thought`（推理 token）/ `action`（工具开始）/ `observation`（工具结果）/ `card`（L2 确认）/ `done`（完成，含 token 用量）

### 核心边界

| 原则 | 含义 |
|------|------|
| 能查不能断 | 可查报告/药典，不可确诊、不可开方 |
| 能建议不能决定 | 可推荐医生/药品，不可替人挂号、替人下单 |
| 能代办不能代确认 | 可发起流程、创建草稿，但敏感操作必须人工确认 |

---

## 二、里程碑总览

| 里程碑 | 目标 | 测试基线 | 状态 |
|--------|------|----------|------|
| **M1 最小对话可用** | 发消息 → 收 LLM 回复（SSE 流式） | — | ✅ 已完成 |
| **M2 工具调用可用** | 查科室 → 返回结果（L1 查询工具 + ReAct） | 50 全绿 | ✅ 已完成 |
| **M3 L2 确认可用** | 创建挂号 → 确认卡片 → 执行成功 | 70 全绿 | ✅ 已完成 |
| **M4 知识库可用** | 医疗知识检索返回结果 | 85 全绿 | ✅ 已完成 |
| **M5 全功能可用** | C 端 4 场景 + B 端 4 场景跑通 | 150 全绿 | ✅ 已完成 |
| **M6 生产就绪** | 集成测试通过，文档完整 | 197 全绿 | ✅ 已完成 |
| **联调前质量收尾** | P1 契约偏差 + mypy strict + 审查缺陷 | 218 全绿 | ✅ 已完成 |
| **M7 深度审查修复** | 第五轮 10 P1 + 12 P2/P3 + 第六轮新发现 2 P1 + 新 P2/P3 修复 | 218 + 新增回归 | 🔄 进行中 |

---

## 三、里程碑详述

### M1：最小对话可用 ✅

前端发消息 → Agent 鉴权 → LLM 推理 → SSE 流式返回，核心链路打通。

- `auth_node` / `intent_node` / `rag_node` / `reply_node` 四节点实现
- SSE 流式（`astream_events` v2，逐 token）；健康检查端点（PG / Redis / LLM）
- MCP Server 启动骨架（stdio transport）；工具 Schema 注册中心（C 端 30 + B 端 9）

**全局关键设计决策**（代码优化完善阶段定案，22 commits，修复 6 CRITICAL + 11 HIGH）

| 决策 | 最终方案 |
|------|---------|
| D1 JWT 校验失败 | 始终返回 401，不降级匿名（严格） |
| D3 MCP 启动失败 | 始终 raise 终止进程（严格） |
| D2 knowledge 鉴权 | 要求登录（B 端 ADMIN 角色待 roles 注入后补） |
| D4 SSE 流式 | 纳入，已用 astream_events 实现 |

### M2：工具调用可用 ✅

**目标**: LLM 能调 L1 工具查数据，`action`/`observation` 事件正确推送，验收链路 `curl 查科室 → action → observation → 回复` 跑通。

**关键交付**
- `tool_caller` 节点：`ToolRegistry.get_tools_by_scope` 过滤 L1 → `bind_tools` → 解析 `tool_calls`；双层防护（只绑 L1 + 即便 LLM 返回 L2/L3 也过滤）；兜底降级 qa 式回答；携带完整对话历史 + 注入上一轮工具结果
- 子图循环：`tool_caller → safety_check → tool_executor`（`MAX_TOOL_ITERATIONS=5`）支撑多步决策；无新工具调用时不覆盖已有 `tool_results`（防关键 bug：第 2+ 轮空结果覆盖首轮查询，reply 拿不到真实数据）
- `tool_executor` 修复 `_execute_local` 缺 `await`（search_knowledge 为异步函数）
- 主图接入 4 子图（triage/registration/consultation/pharmacy），`route_by_intent` 分流
- SSE `action`/`observation` 事件完善（见下方方案调整）
- mock Java 端到端联调：`call_java_api` 补 `_MCP_TOOL_FUNCS` 分发表（37 工具），修复 MCP 工具执行断链

**关键决策与方案调整**
- **M2 范围决策**：只启用 L1 查询工具，L2 整体归 M3（避免确认端点未实现导致流程挂起）
- **SSE 方案两轮调整**：langgraph 1.2.9 无 `emit_custom_event`、`on_tool_start/end` 对直调异步函数不触发 → 改用 `get_stream_writer()` 推自定义事件 → 又发现子图 custom 事件不传播到主图 → **定案：SSE 层从 `updates` 的 `tool_calls`/`tool_results` 重建 action/observation**
- **MCP 过渡实现**：工具执行直调封装函数（非走 MCP 协议），MCP Client 接入归 M6

**验收**: ✅ `查科室 -> action -> observation -> 返回结果`（mock Java E2E），测试 50 全绿。

### M3：L2 确认可用 ✅

**目标**: 创建型操作走 card 确认 → confirm 回调 → 执行 → 返回结果。全链路（含 E2E）已跑通。

**关键交付**
- Redis confirm_token 原子消费：Lua SCAN 前缀匹配 + user_id 校验 + 一次性 GET+DEL（key 格式 `confirm:{session}:{tool}:{token}` 不变）
- `safety_check` 生成时写 Redis（5min TTL）；**Redis 不可用时拒绝 L2 操作**（`redis_unavailable_{tool}` 风险标记），不降级为直接执行
- `tool_caller` 绑定 L1+L2（L3/L4 仍不注册，双层防护保持）
- SSE `card` 事件：`_build_card()`（card_type/confirm_token/session_id/title/summary/expires_at）+ `_TOOL_LABELS` 中文标签；session_id 入口统一生成注入，保证 card 与 done、Redis key 一致
- `/chat/confirm` 端点：原子消费 token → 校验 session → 同步执行 L2 工具；错误码 `CONFIRM_INVALID` / `CONFIRM_EXPIRED` / `SESSION_MISMATCH` / `TOOL_FAILED`；成功 `{code:00000, data:{action_result, message}}`
- 测试：`test_chat_confirm.py` 7 用例 + `test_safety.py` 6 用例 + E2E L2 确认全链路（token 一次性，重复提交过期）

**验收**: ✅ `创建挂号 → card 事件(confirm_token) → /chat/confirm → 执行成功 → 结果返回`，测试 70 全绿。

### M4：知识库可用 ✅

**目标**: 医疗知识检索返回结果。全链路（真实入库 + 检索 + 去重）已跑通。

**关键交付**
- 修复 vectorstore 异步检索（`async_mode=True` + `create_async_engine`）
- 检索 score 归一化 [0,1] + `kb_min_score` 阈值（默认 0.3）；失败降级返回空（不 500），空库自动建 collection
- 入库确定性 ID 去重（文件名+内容哈希 → UUID，`ON CONFLICT DO UPDATE`），读盘+切分 `asyncio.to_thread`
- **embedding 决策**：硅基流动 key 无效（30014）、智谱余额不足（429），选定**阿里云百炼 dashscope**（`text-embedding-v4`）；须 `check_embedding_ctx_length=False` 直传原始文本（百炼不接受 tiktoken token ID 输入）；真实入库+检索 score 0.748
- 测试：`test_rag.py` 8 用例 + `test_ingest.py` 6 用例 + 真实 PG E2E

**遗留（M5 完成）**: 确认结果回写对话上下文（T-M3-L1，见 M5 关键交付）

**验收**: ✅ 真实入库 + 检索 + 去重跑通，测试 85 全绿。

### M5：全功能可用 ✅

**目标**: C 端 4 场景 + B 端 4 场景跑通。

**关键交付**
- 子图业务分化：`tool_caller` 白名单参数（绑定+解析双层过滤），4 子图专属工具集（5/8/5/5，对齐系分 §5.2.1）
- `risk_flags` 注入 reply：`redis_unavailable_{tool}` / `blocked_{tool}` → 中文警告（兑现系分 §7.1）
- B 端权限：中间件注入 `roles`/`dept_id`/`doctor_id`/`hospital_id`；knowledge 入库端点 ADMIN-only（401/403）
- **T-M3-L1 确认结果回写**：Redis `confirm_done:{session}:{tool}:{uuid}`（Lua 一次性消费），confirm 成功后写回执，下一轮 `/chat/stream` 读取注入 system message，确认后追问连续
- B 端 9 工具可调用 + 契约校正：契约表（java_api_map，48 条接口契约）路径对齐系分 V1.1（统一 `/api/b/admin/*`）；`query_patient_history` 安全级别 L1→**L2**（医生授权 + 确认卡片）；启动双校验（validate_contract + validate_tool_references）

**验收**: ✅ C 端 4 场景白名单分化 + B 端权限与 9 工具代码完成，测试 150 全绿。

### M6：生产就绪 ✅

**目标**: 生产级健壮性。8 个任务全部代码完成并提交。

**关键交付**
- **SSE custom 通道定案清理**：stream_mode 收敛为 `["messages", "updates"]`，删除 custom 分支，action/observation 统一从 `tool_results` 反推
- `chitchat_node` 独立节点（闲聊不触发 RAG）；删除零调用零测试的 `compress_messages`（`truncate_messages` 滑动窗口定案，避免 O(n²) 全量摘要与幻觉风险）
- 鉴权去重：中间件 token/parse 单次注入身份，`auth_node` 消费注入值不再重复调 Java
- LLM 降级策略：供应商链式 `deepseek → zhipu → qwen`，配置期决议（不破坏 messages-mode token 流式）
- PostgresSaver 生产持久化：`checkpointer_backend` 配置项（memory 默认 / postgres 生产），`AsyncPostgresSaver` + 连接池全局单例
- `done.usage` + `thought` 事件：done 携带 LLM token 用量，推理模型 `reasoning_content` 推 thought
- **MCP Client 接入（★）**：`tool_executor` 执行链路改走真实 MCP 协议（Client tools/call → Server → dispatcher → 封装函数 → Java REST）；同进程内存传输（复用处理器、无子进程生命周期问题），连接层失败才回退直调（工具执行失败不触发，避免重复执行）；真正跨进程 stdio 留待联调

**验收**: ✅ 8 任务代码完成，测试 197 全绿（新增 21 项），MCP Client 真实协议链路 E2E 验证通过。

### 联调前质量收尾（2026-08-03）✅

**1. 代码审查 3 项缺陷修复**
- **B 端工具子图不可达（HIGH）**：业务子图白名单将 B 端 8 工具滤空 → 白名单仅约束 C 端场景，B 端全量绑定
- **chat_stream Redis 硬依赖（HIGH）**：Redis 故障导致全部对话 500 → confirm_done 读取失败降级为空（与限流 fail-open、L2 转 risk_flags 口径一致）
- **confirm Lua session_id glob 注入（MEDIUM）**：session_id 未转义可越权 SCAN → `_escape_glob` 转义 glob 元字符

**2. mypy strict 基线清零**（142 errors → 0，40 文件）
- `python_version` 3.11→3.13 对齐运行时（消除 numpy 2.5.1 存根解析阻断）
- 全量补齐泛型（`dict`→`dict[str, Any]` 等）、`cast()` 收敛 Any、LLM api_key `SecretStr` 包装
- 过程中修复 **checkpointer 运行时 NameError**（TYPE_CHECKING 导入被模块级注解/表达式运行期求值 → `from __future__ import annotations` + 导入移入函数内）

**3. P1 契约偏差处理**
- 鉴权/限流错误响应对齐系分 §6.1 统一信封 `{code, message, data, traceId}`；`AUTH_FAILED` 拆分为 `AUTH_MISSING`（缺 token）/ `AUTH_INVALID`（token 无效）；`AUTH_EXPIRED` 需 Java token/parse 补过期信号后拆分（当前归 AUTH_INVALID，同为 401）
- `/chat/confirm` 错误路径 HTTP 状态码对齐 §6.2.2：`CONFIRM_INVALID`/`CONFIRM_EXPIRED`/`SESSION_MISMATCH`=**400**、`TOOL_FAILED`=**500**（原固定 200）
- **`/api/knowledge/*` 收敛到系分 §6.5**：合单入库 `POST /api/knowledge/ingest`（file + title 必填 + category/source，单文件 ≤10MB，信封返回 `data:{document_id, title, chunk_count, status}`）；移除 `/ingest/file` + `/ingest/directory` 对外暴露（ingest_directory 保留引擎内部批量工具）；`/search` 补 category 过滤，results 对齐 `rank/score/content/source_doc/source_page/chunk_id` + `total_hits` + `elapsed_ms`；错误统一信封 `INVALID_REQUEST`(400)/`AUTH_INVALID`(401)/`FORBIDDEN`(403)/`INGEST_FAILED`(500)

**当前验证基线**：mypy 0 errors · ruff check/format 通过 · **218 测试全绿**

### M7：深度审查修复（2026-08-03）🔄

**目标**: 第五轮全面审查（三路深挖：L2 confirm 链路 / 中间件与生命周期 / RAG+MCP）发现的 10 P1 + 12 P2/P3，叠加第六轮全面审查（四路并行：安全 / 编排并发 / RAG+MCP / 质量契约）新发现的 2 P1 + 一批 P2/P3，全部修复。**无 P0**；第五轮 4 项 P1 已实测复现，2 项生产必现（SSE finally yield、限流突发失效）；第六轮新发现 2 项 P1 在当前 .env 配置（DEBUG+ALLOW_ANONYMOUS 双开）下即可触发。

**修复进度（截至 `b666441`）**：P1 级 12 项已修 9（NP-1 ✅、P1-7 ✅、NP-2 ✅、P1-10 ✅、P1-1 ✅、P1-2 ✅、P1-3 ✅、P1-4 ✅、P1-8 ✅），待修 3（P1-5/6/9）；测试基线 218 -> 242 全绿，ruff/mypy 通过。

**P1 修复清单**（标注 ✓实测 为已复现验证）：

| # | 问题 | 位置 | 修复方向 |
|---|------|------|---------|
| P1-1 ✓实测 🔄 | Java 失败伪装成功：任何 dict 返回（含 5xx/业务失败信封）都标 success=True，confirm 回"操作成功"，审计记 success | `tool_executor._execute_mcp` | 校验信封 `code != "00000"` 时标 `success=False` 并透传错误 |
| P1-2 ✅ | 多轮 L2 卡片互相覆盖：pending_confirmations 覆盖式写入，先触发的 L2 卡片静默丢失 | `orchestrator/nodes/safety.py` | 按轮次累积列表，或每卡独立 token 管理、逐个消费 |
| P1-3 ✅ | SSE finally 中 yield：客户端断开（GeneratorExit）时抛 RuntimeError，生产必现 | `api/routes/chat.py` line 246 | finally 内 try/except GeneratorExit，done 事件移正常路径 |
| P1-4 ✅ | 限流突发失效：连接池耗尽 fail-open 全放行（300 并发实测）+ ZADD 同 tick member 去重 | `api/middleware/rate_limit.py` + Redis Lua | 降级为进程内内存计数；member 加随机后缀 |
| P1-5 | 无效 token 洪泛绕过限流：JWT 先于 RateLimit 短路，每请求直打 Java token/parse（DoS） | 中间件顺序 | RateLimit 移到 JWT 之前，或 AUTH_INVALID 也计数 |
| P1-6 | 重复入库去重失效：chunk ID = sha256(随机临时文件名+内容)，同文档重复提交无限累积 | `engine/rag/ingest.py` + `api/routes/knowledge.py` | chunk ID 仅用内容哈希；入库做内容哈希存在性检查 |
| P1-7 ✅ | MCP user_id 注入：LLM 工具参数自带 user_id 时原样透传 X-User-Id（匿名路径横向越权） | `mcp_client/client.py` + `mcp_server/server.py` | user_id 仅从 JWT 可信源注入，参数中 user_id 一律剥离 |
| P1-8 ✅ | NaN 相似度钳制 1.0 置顶：`min(1.0, float('nan'))` 返回 1.0，污染检索 | `engine/rag/search.py` line 52 | 钳制前 `math.isnan` 检查，NaN 过滤或置 0 |
| P1-9 | 同 session 并发竞态：checkpointer 锁 per-instance，并发请求互相覆盖 state | `orchestrator/checkpointer.py` | session 级 asyncio.Lock 串行化，或接入层去重 |
| P1-10 ✅ | .env 配置风险：DEBUG=True + ALLOW_ANONYMOUS=true 同时开启（跳过全部鉴权）+ 真实 API key 入库 | `.env` + `api/middleware/jwt_auth.py` | DEBUG 与 ALLOW_ANONYMOUS 互斥校验；密钥移出仓库 |

**P2/P3 修复清单**：

- 冒号前缀跨会话 SCAN 越权（✓实测）：session "abc" 可匹配读删 "abc:def" 的 key → session_id 加边界定界符
- confirm token 先删后执行 + 幂等键无复用：消费后立即 DEL，失败重试时新 UUID 幂等键 → 同一业务重复执行 → 执行成功后再删 token，失败复用原幂等键（幂等键参数已预留，接入调用方）
- confirm_done 回执只注入 tool label 不注入 action_result → 回执补充操作结果摘要
- 审计 confirm_method/trigger 恒为 none/agent → 补齐真实取值来源
- ingest 先 `await file.read()` 全量进内存再校验 10MB → 先校验大小再读取
- MCP 连接生命周期：CancelledError 不触发 _connect_inner 清理；close_mcp_client 不持 _connect_lock → 两处补齐
- jwt data 缺 userId 时 roles=[] 被判已鉴权（`is not None` 对空列表为真）→ 显式判空
- checkpointer thread_id 无用户归属校验 → 绑定 user_id 校验
- resolve_api 不校验未填充的 `{param}` 占位符 → 解析后校验
- 死参数 drug_name（check_allergy_risk / check_duplicate_medication schema 要求但不使用）→ 从 schema 移除或实现语义
- 契约表冗余：query_patient_history:medications 与 query_patient_medications 同路径 → 合并去重
- Lua SCAN 遍历阻塞单线程 → 后续如并发瓶颈再评估（P3 暂缓）

**第六轮全面审查补充（2026-08-03，4 路并行代理）**：

本轮独立验证第五轮 10 P1 + 11 P2 全部真实（契约表冗余 1 项已确认合并修复），并发现 2 项第五轮遗漏的 P1 越权 + 一批新 P2/P3。新发现 2 项 P1 在当前 .env 配置下可直接触发。

**新发现 P1（第五轮遗漏，标 NP）**：

| # | 问题 | 位置 | 修复方向 |
|---|------|------|---------|
| NP-1 ✅ | 匿名用户经 `X-Scope` 头越权访问 B 端工具：scope 完全由客户端头控制且不与 JWT 绑定，匿名 + `X-Scope: b_end` 可调全部 B 端工具；与 P1-7 联动可形成"匿名 + 注入 user_id + B 端 scope"完整越权链（以任意医生身份查患者档案 / 生成病历草稿） | `api/middleware/jwt_auth.py:47,87-89` + `orchestrator/nodes/tool_caller.py:100` | 匿名路径强制 scope=c_end；或 tool_caller 对 user_id=None 拒绝 B 端工具 |
| NP-2 ✅ | DEBUG=True 完全绕过知识库鉴权：`_require_auth` 在 debug 为真时直接 return None，跳过登录 + ADMIN 校验，未认证可向医疗知识库上传任意文档投毒 RAG（直接医疗安全风险） | `api/routes/knowledge.py:52` | DEBUG 不绕过写操作鉴权（仅跳过登录保留角色校验）；或仅 DEBUG+ALLOW_ANONYMOUS 双开时跳过登录 |

**新发现 P2**：

- jwt_auth 每请求新建 `httpx.AsyncClient` 不复用全局连接池，每请求 TCP 握手（`api/middleware/jwt_auth.py:157`）→ 复用 `java_client.get_client()`
- PGVector async engine lifespan 关闭未 dispose，reload/重启连接泄漏（`app/main.py:84-96` + `engine/rag/vectorstore.py`）→ 增 `close_vectorstore()` 并在 lifespan 关闭段调用
- `_check_pg` 健康检查只验 PGVector 实例创建不验连通，DB 宕机仍报 ok（`app/main.py:111-118`）→ 执行 SELECT 1 实际查询
- `idempotency_key` 每次新生成，无调用方传入，重试无法去重——P2"幂等键无复用"的根本原因（`infrastructure/java_client.py:110-111`）→ confirm_token 时生成确定性 key 并透传至 call_java_api
- `call_java_api` 无重试：tenacity 已声明依赖但全项目零引用，Java 瞬时抖动直接失败（`java_client.py:113-163` + `pyproject.toml:33`）→ 幂等 GET 加 tenacity 重试，或移除未用依赖
- `query_reports` 两次独立 Java 调用串行未 gather，延迟翻倍（`mcp_server/tools/health.py:69-78`）→ 改 asyncio.gather
- `confirm_done` 回执无 user_id 校验，知道 session_id 即可跨用户读 action_result（含 PHI）（`infrastructure/cache/redis_client.py:214`）→ 增 user_id 校验或 key 加 user_id 段
- 异常 `str(e)` 直接透传 LLM/SSE observation，可能泄露内部路径/连接串（`orchestrator/nodes/tool_executor.py:93,153`）→ 对外脱敏，详情仅日志
- CORS `allow_credentials=True` 对 Bearer API 不必要，且 `allow_headers=["*"]` 允许跨域设 X-Scope（`app/main.py:161-167`）→ 关闭 credentials，限定 headers 白名单

**新发现 P3/规范**：

- 函数 >50 行 19 处，2 处严重：`_sse_generator`(118行)、`call_java_api`(117行)；`_sse_generator` 嵌套最深 9 层（规范≤4）→ 拆 `_handle_updates` 子函数
- 6 处缺 docstring（3 个 middleware.dispatch、schema_registry.get_tool、server._run、_common._caller）
- SSE 契约偏差：`error`/`thought` 事件未在系分 §6.2.1 表；`done` 多 `trace_id` 字段；`/health` 不走统一信封
- DRY：统一信封错误响应在 4 处重复（chat/knowledge/jwt_auth/rate_limit）→ 抽 `envelope.error_response()`；工具中文标签/card_type 映射各维护两处 → 同步风险
- 私有函数 `_execute_mcp` 被 `chat.py` 跨模块引用（破封装，`chat.py:24`）
- 硬编码应入 settings：`_AUTH_TIMEOUT`/`MAX_DATA_CHARS`/`MAX_TOOL_ITERATIONS`
- 默认值不安全：`APP_VERSION` 硬编码、`AGENT_HOST=0.0.0.0`、`.env.example` 默认 DEBUG=True+ALLOW_ANONYMOUS=true
- 行内导入 2 处（`redis_client.py:295` import time、`ingest.py:42` import importlib）
- `call_java_api` 的 `scope` 参数完全死代码；`_wrap` 静默丢弃未知参数；多处 `if x:` 误判 id=0

**已核查无问题（防线可靠，不修，第六轮独立交叉验证确认）**：Lua 原子消费 5 并发仅 1 成功（无超卖）；`_escape_glob` glob 元字符全转义；Redis 故障 fail-closed 剔除 L2；tool_results 跨轮自累积 + 跨对话 None 清场正确；路径遍历防护（文件名仅取后缀白名单，不参与路径拼接）；SQL 注入防护（psycopg 参数化 + pgvector API）；confirm_token 不可伪造/重放（UUID4 + 原子 GET+DEL + user_id 校验）；L3/L4 工具双层防护不泄露（registry 过滤 + server 求交）；医疗安全声明流式/非流式均只注入一次；工具 Schema 注册一致（39 工具 / L1-L4 标注 / 启动双校验）；MCP 握手时序；category JSONB 语法；契约表 48 条双校验。

**修复顺序**：~~NP-1/NP-2 + P1-7/P1-10 先行~~ ✅已完成（`8cfd16c`/`c7ca804`/`d4971d0`）；P1-1 → P1-2 → P1-3 → P1-4 次之（直接误导用户业务结果判断且改动面小）；P1-6/P1-8 为独立小改；P1-5/P1-9 收尾。每项修复配套回归测试，基线目标 218 + 新增用例全绿（当前 223）。

---

## 四、当前状态与待办

### 当前基线

| 维度 | 状态 |
|------|------|
| 致命/严重 bug | ✅ 6 CRITICAL + 11 HIGH 全部修复；🔄 M7 修复中：P1 级 12 项已修 4（NP-1/P1-7/NP-2/P1-10），进行中 P1-1，待修 7（详见 §三 M7） |
| 安全 | ✅ JWT 校验、限流隔离、路径遍历、硬编码密钥、CORS、PHI 脱敏 |
| 异步模型 | ✅ RAG/搜索异步化，同步 I/O 用 to_thread 包装 |
| 规范/类型/DRY | ✅ 函数 ≤50 行、类型注解、子图去重、reply 不 mutate |
| ruff / mypy | ✅ 全项目 0 errors / strict 清零（142 errors → 0） |
| 测试 | ✅ 223 全绿（含 NP-1/P1-7/NP-2/P1-10 修复断言），ruff/mypy 提交前把关 |

> **说明**：`tests/` 目录按 `.gitignore` 不入库，全部单测为本地验证资产。

### 待联调验证（依赖真实环境）

| 项 | 说明 |
|----|------|
| 真实 LLM 决策质量 | L2 挂号全链路已用 mock 两轮决策单测验证，真实 glm-4 偏保守需实测；多供应商降级、thought token、意图/工具调用质量 |
| Java 业务 API 全量联调 | C 端 4 场景 + B 端 9 工具、B 端 L2 确认卡片（query_patient_history）、契约表单一改动点校正 |
| JWT 真实校验 | 对接 Java token/parse 接口；`AUTH_EXPIRED` 错误码需 Java 补过期信号后拆分 |
| SSE 真实流式 | token 级流式实际输出、前端渲染联调 |
| PostgresSaver 生产验证 | `CHECKPOINTER_BACKEND=postgres` 多 worker 会话共享 |
| MCP Client 跨进程 stdio | 外部 MCP Server 消费工具的真实形态验证 |

### 遗留清理项（2026-08-03 全面审查发现，同日全部处理完毕 ✅）

**P1 — SSE 事件字段对齐系分 §6.2.1（✅ 已处理）**：
- `action` 补 `label`：`_TOOL_LABELS` 补全 39 个工具中文标签，action 事件携带 `{tool, label, arguments}`
- `observation` 对齐契约：重建为 `{tool, status, result, summary, duration_ms}`；`tool_executor` 结果补记 `duration_ms`，SSE 层生成 summary（成功统计条数 / 失败取错误消息）
- `card` 补 `details`：按 card_type 从 `tool_arguments` 提取 ID/动作类字段；名称类字段（doctor_name 等）依赖确认执行后回填，确认前不可得，系分文档已注明降级展示
- 系分文档去重：§5.12 SSE 格式示例合并指向 §6.2.1；§6.2.1 card_type 枚举表补 `confirm_drug_receipt` / `confirm_waitlist` / `confirm_patient_history`；`done` 补 `usage` 字段

**死代码清理（✅ 已处理）**：
- 删除 `app/engine/llm/prompts/*.txt`（含无引用包）、settings 5 个死字段（`kb_ingest_root` / `mcp_transport` / langsmith 三项）、`tests/eval` 空包、`.jbeval/datasets` 空目录、`data/c_end/*.md`
- `call_java_api` 移除从未传参的 `arguments` 参数（docstring 同步清理）

**已知技术债（✅ 已处理）**：
- B 端 4 个聚合工具（query_patient_history / recommend_care / check_contraindication / check_drug_interaction）改 `asyncio.gather` 并发 + 部分失败隔离（`return_exceptions=True` + 失败项转 `AGGREGATE_FAILED`）
- 幂等键：`call_java_api` 新增 `idempotency_key` 可选参数，重试同业务操作时可复用键（当前无重试路径，默认行为不变）
- `check_drug_interaction`：Java `GET /admin/drugs` 契约仅支持单数 `drug_name`，改为逐药并发查询（消除对 Java 逗号分隔扩展的隐性依赖），返回 `drug_info` 由单 dict 变 list

---

## 五、风险与依赖

### 外部依赖

| 依赖项 | 提供方 | 状态 | 风险 |
|--------|--------|------|------|
| Java C 端 token/parse | Java 团队 | ✅ 已完成 | 低 |
| Java B 端 token/parse | Java 团队 | ✅ 已完成 | 低 |
| Java C 端业务 API | Java 团队 | ❌ 待开发 | **高** |
| Java B 端业务 API | Java 团队 | ❌ 待开发 | **高** |

### 技术风险

- MCP Client 已接入（M6-C1），内存传输真实协议链路通过测试；跨进程 stdio 形态待联调验证
- LLM API 响应延迟需监控；PostgreSQL 需启动 pgvector 扩展
- SSE 事件字段契约已与系分 §6.2.1 对齐（遗留清理项已处理），联调时验证前端渲染

### 缓解措施

**Mock 开发模式**: 先对齐 API 契约，Agent 基于契约 Mock 开发，Java API 就位后替换为真实调用。

---

## 六、团队协作

### 与 Java 团队对齐
- 每日同步 Java API 开发进度与接口契约变更
- 每周对齐系分文档更新与联调时间

### 与前端团队对齐
- M1 完成后：前端集成 SSE 流式输出
- M3 完成后：前端集成 L2 确认卡片
- M5 完成后：端到端全流程测试
- 联调前：SSE 事件字段已按 §6.2.1 对齐（action.label / observation.status/result/summary/duration_ms / card.details），按此契约联调前端渲染

---

## 七、配置与部署

### 关键环境变量（`.env`，完整键以 `.env.example` 为准）

```bash
JAVA_BASE_URL=http://localhost:8080
LLM_PROVIDER=deepseek                 # deepseek / zhipu / qwen，链式降级
ZHIPU_API_KEY=your_key
PG_HOST=localhost
PG_PASSWORD=your_password             # 不再硬编码，必须配置
REDIS_HOST=localhost
CONFIRM_TOKEN_TTL=300
RATE_LIMIT_PER_MINUTE=20
CORS_ORIGINS=["http://localhost:5173"]  # 生产收敛
CHECKPOINTER_BACKEND=memory           # memory / postgres（生产）
DASHSCOPE_EMBEDDING_API_KEY=your_key  # 阿里云百炼 embedding（text-embedding-v4）
KB_MIN_SCORE=0.3                      # 知识库检索阈值
# KB_INGEST_ROOT 已废弃：knowledge API 收敛后无引用，见遗留清理项
```

### 启动方式

```bash
# 推荐（无热重载）
uvicorn app.main:app --host 0.0.0.0 --port 8081

# 或
python -m app.main
```

### 部署要点

**启动依赖链**: PostgreSQL(5432) → Java(8080) → Agent(8081) → 前端(8000/8001)。Agent 不内嵌 Java，通过 `JAVA_BASE_URL` 调用；与 Java 共用同一套 PG/Redis。

**pgvector 扩展**: 目标 PG 必须执行 `CREATE EXTENSION vector`（向量库首次建表依赖），否则启动报错。

**生产部署**（`DEBUG=False` 必须关闭 reload）:
```bash
nohup .venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8081 > agent.log 2>&1 &
```
推荐 systemd 托管（开机自启 + `Restart=always`），`EnvironmentFile` 指向 `.env`。

**常见问题**: 端口用 `AGENT_HOST/AGENT_PORT`（非旧 HOST/PORT）；MCP 未启用则 `pip install mcp`；连不上 PG 核对 `PG_*`。

---

**制定人**: Claude Code
**审核人**: 项目负责人
**最后更新**: 2026-08-03（第六轮全面审查：4 路并行代理验证第五轮 10 P1 + 11 P2 全部真实 + 契约表冗余 1 项已修复；新发现 2 P1 越权 [匿名 X-Scope 越权 B 端 / DEBUG 绕过知识库鉴权] + 一批新 P2/P3；交叉验证确认 9 项防线可靠。M7 修复清单与优先级同步更新）
